/** @type {import('next').NextConfig} */
const nextConfig = {
  // Configurar imagens de domínios remotos
  // images: {
  //     remotePatterns: [
  //         {
  //             protocol: 'https',
  //             host: 'www.pexels.com',
  //         }
  //     ]
  // }
};

// Use the following line to enable the experimental server components features

export default nextConfig;
