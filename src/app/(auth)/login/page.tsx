// A pasta (auth) é um agrupador de páginas de autenticação, ele não é considerado no roteamento

const Login = () => {
  return (
    <div>
      <h1>Login</h1>
      <p>This is the Login page</p>
    </div>
  );
};

export default Login;
